class Item {
  content: string;
  id: number;
  title: string;

  constructor(content: string, id: number, title: string) {
    this.content = "content";
    this.id = 0;
    this.title = "title";
  }
}

export default Item;
